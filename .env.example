# ChipNest 运行配置示例（全部为可选覆盖项）
# dev: 控制台输出日志；prod: 仅落盘日志
CHIPNEST_ENV=dev
# 后端监听端口（Electron 打包后也指向它）
CHIPNEST_PORT=8765
# 数据库文件完整路径（默认 backend/data/chipnest.db）
# CHIPNEST_DB=D:\somewhere\chipnest.db
# 日志目录（默认 backend/logs）
# CHIPNEST_LOG_DIR=D:\somewhere\logs
